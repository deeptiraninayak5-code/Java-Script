// let class = "Hello world";
// // Class is a reserved keyword in JavaScript, so it cannot be used as an identifier. This will result in a syntax error. 
// let if = "True";
// // If is also a reserved keyword in JavaScript, so it cannot be used as an identifier. This will also result in a syntax error. 
// let return = 5;
// // Return is also a reserved keyword in JavaScript, so it cannot be used as an identifier. This will also result in a syntax error. 

let  myVariable = 10;
// This is a valid identifier and can be used without any issue. 

// They look like keywords but aren't reserved. 
// let undefined = 5;  // ⚠️ Works but NEVER do this!
// let Infinity = 10; // ⚠️ Works but NEVER do this!
